# Git Notes

Common Commands

```bash
git add <file>
git add -A
git commit -m '<msg>'
git status

git branch
git checkout -b <branchName>
git merge <branch>

git pull origin <branchName>
git push origin <branchName>

git log						# 'q' to quit
```